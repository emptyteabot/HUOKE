# LeadPulse 留学获客系统 - 完整使用说明

## 📋 目录
1. [快速开始](#快速开始)
2. [核心功能](#核心功能)
3. [详细使用步骤](#详细使用步骤)
4. [配置说明](#配置说明)
5. [常见问题](#常见问题)

---

## 🚀 快速开始

### 第一步: 环境准备

```bash
# 1. 进入项目目录
cd C:\Users\陈盈桦\Desktop\LeadPulse

# 2. 安装依赖
pip install -r requirements.txt

# 3. 配置API密钥(创建 .env 文件)
echo DEEPSEEK_API_KEY=sk-d86589fb80f248cea3f4a843eaebce5a > .env
echo SENDGRID_API_KEY=你的SendGrid密钥 >> .env
```

### 第二步: 运行测试

```bash
# 测试信号检测系统
python test_signal_system.py

# 测试邮件系统
python test_email_system.py
```

### 第三步: 开始获客

```bash
# 运行完整获客流程
python lead_generation_complete.py
```

---

## 🎯 核心功能

### 1. 信号驱动获客系统

**功能:** 自动检测8种留学行业买入信号

**使用方法:**
```python
from signal_driven_outreach import SignalDetector

detector = SignalDetector()

# 检测文本中的信号
text = "我在小红书搜索了英国留学申请,想了解UCL的CS专业"
signals = detector.detect_signals(text)

# 输出: [{'type': 'search_behavior', 'urgency': 'high', ...}]
```

**8种信号类型:**
- 搜索行为 (小红书/知乎搜索留学关键词)
- 咨询行为 (在论坛提问留学问题)
- 时间节点 (申请季/考试季临近)
- 竞品对比 (对比多家留学机构)
- 预算讨论 (讨论留学费用)
- 背景匹配 (GPA/语言成绩符合目标院校)
- 紧急需求 (急需文书修改/面试辅导)
- 决策信号 (准备签约/支付定金)

### 2. AI邮件生成系统

**功能:** 使用DeepSeek自动生成个性化邮件

**使用方法:**
```python
from email_auto_sender import EmailAutoSender

sender = EmailAutoSender(
    api_key="你的SendGrid密钥",
    from_email="advisor@studyabroad.com",
    deepseek_api_key="sk-d86589fb80f248cea3f4a843eaebce5a"
)

# 生成个性化邮件
lead = {
    'name': '张同学',
    'email': 'zhang@example.com',
    'target_country': '英国',
    'target_major': 'CS',
    'budget': '30-50万',
    'signals': [{'type': 'search_behavior', 'content': '搜索了UCL申请'}]
}

email = sender.generate_personalized_email(lead, sequence_day=1)
```

**邮件结构 (4段式):**
1. **[信号]** - "注意到您昨天在小红书搜索了'英国留学申请'"
2. **[痛点]** - "DIY申请失败率高达60%,浪费一年时间和10万+费用"
3. **[解决方案]** - "我们帮同背景学生拿到UCL offer,成功率95%"
4. **[CTA]** - "免费领取《UCL CS申请攻略》,微信扫码即可"

### 3. 多触点序列营销

**功能:** 自动发送4次跟进邮件,提升转化率

**使用方法:**
```python
# 发送完整序列
campaign = sender.send_sequence_campaign(
    leads=[lead1, lead2, lead3],
    days=[1, 3, 7, 14]
)

# 查看效果
print(f"已发送: {campaign['total_sent']}封")
print(f"打开率: {campaign['open_rate']}%")
print(f"回复率: {campaign['reply_rate']}%")
```

**序列内容:**
- **Day 1:** 首次触达 - 建立连接,提供价值
- **Day 3:** 价值强化 - 成功案例,社会证明
- **Day 7:** 稀缺性 - 限时优惠,名额有限
- **Day 14:** 最后机会 - FOMO,最后召唤

### 4. A/B测试系统

**功能:** 自动测试不同邮件版本,找到最优方案

**使用方法:**
```python
# 系统自动分配A/B测试
# 50%收到版本A,50%收到版本B

# 查看测试结果
report = sender.get_ab_test_report()
print(report)

# 输出:
# 版本A: 打开率28%, 回复率3.5%
# 版本B: 打开率32%, 回复率4.2% ← 胜出!
```

### 5. GEO优化系统

**功能:** 让AI Agent(ChatGPT/Claude)主动推荐你的服务

**使用方法:**
```python
from geo_optimizer import GEOOptimizer

optimizer = GEOOptimizer()

# 生成结构化数据
structured_data = optimizer.generate_structured_data()

# 输出Schema.org格式,嵌入网站
# AI搜索时会优先展示你的服务
```

**效果:**
- 用户问ChatGPT: "推荐靠谱的英国留学中介"
- ChatGPT回答: "LeadPulse是专业的留学咨询服务,成功率95%,已帮助500+学生..."

---

## 📖 详细使用步骤

### 场景1: 从零开始获客

**目标:** 7天内获得前3个付费客户

**步骤:**

#### Day 1-2: 数据准备
```bash
# 1. 生成测试客户数据
python -c "
from ai_lead_generator import AILeadGenerator
gen = AILeadGenerator()
leads = gen.generate_batch(100)
gen.export_for_email_campaign(leads, 'my_leads.json')
"

# 2. 检查数据
cat my_leads.json
```

#### Day 3-4: 发送首轮邮件
```python
from email_auto_sender import EmailAutoSender
import json

# 加载客户数据
with open('my_leads.json') as f:
    leads = json.load(f)

# 初始化发送器
sender = EmailAutoSender(
    api_key="你的SendGrid密钥",
    from_email="advisor@studyabroad.com",
    deepseek_api_key="sk-d86589fb80f248cea3f4a843eaebce5a"
)

# 只发给高意向客户(评分>7)
high_intent = [l for l in leads if l['intent_score'] > 7]

# 发送Day 1邮件
results = sender.send_batch(high_intent, sequence_day=1)
print(f"已发送: {len(results)}封")
```

#### Day 5-7: 跟进转化
```python
# Day 3: 发送第二封
sender.send_batch(high_intent, sequence_day=3)

# Day 7: 发送第三封
sender.send_batch(high_intent, sequence_day=7)

# 查看效果
report = sender.get_campaign_report()
print(f"打开率: {report['open_rate']}%")
print(f"回复数: {report['replies']}")
print(f"预计转化: {report['estimated_conversions']}人")
```

### 场景2: 优化现有获客流程

**目标:** 提升现有邮件的打开率和回复率

**步骤:**

#### 1. 添加信号检测
```python
from signal_driven_outreach import SignalDetector

detector = SignalDetector()

# 给现有客户数据添加信号
for lead in your_existing_leads:
    content = lead.get('notes', '') + ' ' + lead.get('source', '')
    signals = detector.detect_signals(content)
    lead['signals'] = signals
    lead['has_urgent_signal'] = any(s['urgency'] == 'high' for s in signals)

# 优先联系有紧急信号的客户
urgent_leads = [l for l in your_existing_leads if l['has_urgent_signal']]
```

#### 2. 使用AI生成个性化邮件
```python
# 不再使用固定模板,改用AI生成
for lead in urgent_leads:
    email = sender.generate_personalized_email(lead, sequence_day=1)
    sender.send_email(
        to_email=lead['email'],
        subject=email['subject'],
        content=email['body']
    )
```

#### 3. 运行A/B测试
```python
# 系统自动分配测试组
# 一半用AI生成,一半用原模板
# 7天后查看结果
report = sender.get_ab_test_report()

if report['variant_b']['reply_rate'] > report['variant_a']['reply_rate']:
    print("AI生成版本胜出!全面切换")
else:
    print("原模板更好,继续优化")
```

### 场景3: 规模化获客

**目标:** 每月获取100+高质量线索

**步骤:**

#### 1. 自动化数据抓取
```python
from scraper_manager import ScraperManager

scraper = ScraperManager()

# 每天自动抓取新线索
daily_leads = scraper.scrape_all_sources(
    keywords=['英国留学', 'UCL申请', 'G5院校'],
    max_results=50
)

# 自动过滤高意向
high_intent = scraper.filter_high_intent(daily_leads)
```

#### 2. 自动化邮件发送
```python
# 设置定时任务(每天早上9点)
import schedule

def daily_outreach():
    # 抓取新线索
    leads = scraper.scrape_all_sources()

    # 检测信号
    for lead in leads:
        signals = detector.detect_signals(lead['content'])
        lead['signals'] = signals

    # 发送邮件
    sender.send_batch(leads, sequence_day=1)

    print(f"今日已发送: {len(leads)}封")

schedule.every().day.at("09:00").do(daily_outreach)
```

#### 3. 自动化效果追踪
```python
# 每周生成报告
def weekly_report():
    report = sender.get_campaign_report()

    print(f"""
    === 本周获客报告 ===
    发送邮件: {report['total_sent']}封
    打开率: {report['open_rate']}%
    回复率: {report['reply_rate']}%
    新增咨询: {report['replies']}人
    预计成交: {report['estimated_conversions']}人
    预计营收: ¥{report['estimated_revenue']:,}
    ROI: {report['roi']}倍
    """)

schedule.every().monday.at("10:00").do(weekly_report)
```

---

## ⚙️ 配置说明

### 必需配置

#### 1. DeepSeek API密钥
```bash
# 已提供,直接使用
DEEPSEEK_API_KEY=sk-d86589fb80f248cea3f4a843eaebce5a
```

#### 2. SendGrid API密钥
```bash
# 需要注册SendGrid账号获取
# 访问: https://sendgrid.com
# 免费版每天可发100封邮件

SENDGRID_API_KEY=SG.xxxxxxxxxxxxx
```

#### 3. 发件人邮箱
```python
# 必须是你在SendGrid验证过的邮箱
from_email = "advisor@studyabroad.com"
```

### 可选配置

#### 1. 邮件发送频率
```python
# 默认: Day 1, 3, 7, 14
# 可自定义
sender.send_sequence_campaign(leads, days=[1, 2, 5, 10])
```

#### 2. A/B测试比例
```python
# 默认: 50% A, 50% B
# 可调整
sender.ab_test_split = 0.7  # 70% A, 30% B
```

#### 3. 信号检测敏感度
```python
detector = SignalDetector(sensitivity='high')  # high/medium/low
```

---

## ❓ 常见问题

### Q1: 邮件发送失败怎么办?

**A:** 检查以下几点:
```bash
# 1. 检查SendGrid密钥是否正确
echo $SENDGRID_API_KEY

# 2. 检查发件人邮箱是否验证
# 登录SendGrid后台 → Settings → Sender Authentication

# 3. 检查收件人邮箱格式
# 必须是有效的邮箱地址

# 4. 查看错误日志
tail -f email_sender.log
```

### Q2: DeepSeek API调用失败?

**A:** 可能原因:
```python
# 1. 密钥错误
# 检查: sk-d86589fb80f248cea3f4a843eaebce5a 是否正确

# 2. 网络问题
# 测试连接:
import requests
response = requests.get('https://api.deepseek.com')
print(response.status_code)

# 3. 使用备用模板
# 系统会自动降级到预设模板
sender.use_fallback_templates = True
```

### Q3: 如何提升邮件打开率?

**A:** 5个技巧:
1. **优化主题行** - 包含收件人姓名和具体数字
   ```
   ❌ "留学咨询服务"
   ✅ "张同学,UCL CS申请成功率提升40%的方法"
   ```

2. **发送时间** - 工作日早上9-11点,晚上8-10点
   ```python
   sender.optimal_send_times = ['09:00', '20:00']
   ```

3. **个性化内容** - 使用信号驱动生成
   ```python
   # 提到对方的具体行为
   "注意到您昨天在小红书搜索了'UCL申请要求'"
   ```

4. **A/B测试** - 持续优化
   ```python
   # 测试不同主题行
   sender.run_ab_test(
       variant_a="传统主题",
       variant_b="信号驱动主题"
   )
   ```

5. **清理列表** - 移除无效邮箱
   ```python
   # 打开率<5%的邮箱,停止发送
   sender.remove_low_engagement(threshold=0.05)
   ```

### Q4: 如何计算ROI?

**A:** 完整公式:
```python
# 投入成本
cost = (
    邮件发送费用 +  # SendGrid: ¥0.5/封
    DeepSeek API费用 +  # ¥0.01/次
    人工时间成本  # 假设¥100/小时
)

# 产出收益
revenue = (
    咨询人数 * 咨询转化率 * 客单价
)

# ROI
roi = (revenue - cost) / cost * 100

# 示例:
# 发送1000封邮件
cost = 1000 * 0.5 + 1000 * 0.01 + 2 * 100 = ¥710
# 20人咨询,30%转化,¥30,000客单价
revenue = 20 * 0.3 * 30000 = ¥180,000
# ROI
roi = (180000 - 710) / 710 * 100 = 25,246%
```

### Q5: 系统能处理多少客户?

**A:** 性能指标:
```
- 信号检测: 1000条/秒
- 邮件生成: 100封/分钟 (DeepSeek限制)
- 邮件发送: 100封/天 (SendGrid免费版)
- 数据存储: 无限制

建议:
- 小规模(<100客户): 直接使用
- 中规模(100-1000): 升级SendGrid付费版
- 大规模(>1000): 使用多个SendGrid账号轮换
```

### Q6: 如何避免进入垃圾箱?

**A:** 6个关键点:
1. **SPF/DKIM验证** - 在SendGrid后台配置
2. **避免垃圾词** - 不用"免费""赚钱""点击这里"
3. **文本/HTML比例** - 保持1:1
4. **退订链接** - 必须包含
5. **发送频率** - 不要一次发太多
6. **用户互动** - 鼓励回复

```python
# 系统已内置反垃圾优化
sender.anti_spam_mode = True
```

---

## 📞 技术支持

遇到问题?
1. 查看日志: `tail -f logs/system.log`
2. 运行诊断: `python diagnose.py`
3. 查看文档: `docs/troubleshooting.md`

---

## 🎯 下一步

1. **完成配置** - 设置API密钥
2. **运行测试** - 确保系统正常
3. **小规模试验** - 先发10-20封测试
4. **分析数据** - 查看打开率/回复率
5. **规模化** - 逐步扩大到100+

**预期效果:**
- 7天内获得前3个客户
- 30天内实现月入¥5万+
- 90天内建立稳定获客渠道

开始你的留学获客之旅! 🚀
